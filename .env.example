PHONE_NUMBER_ID=
KAPSO_API_KEY=
WABA_ID=
# Optional: custom API endpoint (defaults to https://api.kapso.ai/meta/whatsapp)
WHATSAPP_API_URL=
# Optional: Kapso workflow id that owns this inbox's conversations
# (defaults to "wapido-flow v1.0 (Prod)"). Needed for workflow status & handoff.
KAPSO_WORKFLOW_ID=
